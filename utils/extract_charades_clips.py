import argparse

def main(videos_dir, video_anno_path, verb_class_path, mode, target_dir):

if __name__ == '__argparse__':
    parser = argparse.ArgumentParser()
    parser.add_argument()
    args = parser.parse_args()

    main(videos)